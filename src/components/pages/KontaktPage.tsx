"use client";

import ContactSection from "@/components/nmbd/ContactSection";

/** Dedicated contact route — same form/section as homepage #contact. */
export default function KontaktPage() {
  return <ContactSection />;
}
